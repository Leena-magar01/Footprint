steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/ecopilot-frontend',
      '--build-arg', 'VITE_API_URL=$_VITE_API_URL',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/ecopilot-frontend'
